/**
 * (simulators) route group layout
 *
 * Wraps /simulator and /simulator/[slug]. Add simulator-specific navigation,
 * breadcrumbs, or providers here without affecting other sections.
 */
export default function SimulatorsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
