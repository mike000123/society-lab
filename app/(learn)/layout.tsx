/**
 * (learn) route group layout
 *
 * Wraps /learn and /learn/[slug]. Add learn-specific navigation,
 * breadcrumbs, or providers here without affecting other sections.
 */
export default function LearnLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
